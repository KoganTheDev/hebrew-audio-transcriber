# Development dependencies (tests, linting) are declared in pyproject.toml
# under [project.optional-dependencies].dev - see requirements.txt for why
# this file is a pointer rather than a list.
-e .[dev]
