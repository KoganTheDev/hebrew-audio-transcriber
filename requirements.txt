# Runtime dependencies are declared in pyproject.toml, which is the single
# source of truth - this file just installs the project itself so that
# `pip install -r requirements.txt` keeps working for anyone who reaches for
# it out of habit. Editing this file cannot change what gets installed; edit
# [project].dependencies in pyproject.toml instead.
-e .
